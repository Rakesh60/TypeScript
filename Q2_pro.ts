
function calculateArea(length: number, breadth: number): number {
    return length * breadth;
  }
  
 
 
  
let len:number=50;
let brd:number=50;


calculateArea(len,brd)
console.log(`The Area of Rectangle length:${len} and breadth:${brd} is :${}`)